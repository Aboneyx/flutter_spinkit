name: example
description: A new Flutter project.

publish_to: 'none'

environment:
  sdk: ">=2.18.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_spinkit:
    path: ../
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
